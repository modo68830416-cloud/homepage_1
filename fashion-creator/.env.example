# AI Gateway provider selection (src/ai/providers/registry.ts).
# "demo" is the only implemented provider today; the others are reserved
# names for future integrations and currently fall back to "demo".
NEXT_PUBLIC_AI_PROVIDER=demo
# openai | gemini | flux | kling | runway | fal | replicate
